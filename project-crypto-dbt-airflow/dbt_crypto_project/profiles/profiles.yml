dbt_crypto_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: crypto
      schema: core         # base schema for dev; folders can override
      threads: 4
      connect_timeout: 10
