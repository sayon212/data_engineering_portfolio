version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: market_metrics
      - name: coin_historical

models:
  - name: market_data
    description: "Clean data"
    columns:
      - name: coin_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
      - name: coin_symbol
        data_tests:
          - unique
          - not_null